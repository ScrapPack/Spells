using UnityEngine;

/// <summary>
/// OnGUI display for character selection screen.
/// Not used by the current box arena flow — reserved for future full-lobby support.
/// </summary>
public class CharacterSelectUI : MonoBehaviour
{
    // Stub — CharacterSelectManager removed. Implement when a lobby/char-select
    // flow is added back. Wire class picks directly into BoxArenaBuilder inspector
    // fields (player1ClassData / player2ClassData) in the meantime.
}
